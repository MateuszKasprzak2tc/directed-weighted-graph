public class Graph {
    Vertex vertex;
    Edge edge;

    public void addEdge(edge.vertex.key, vertex.key, edge.weight){
        Edge newEdge = new Edge();
        newEdge.vertex.key = 
    }
}
